package com.opps.concept;

public class DataType {

	public static void main(String[] args) {
		// Without main method we can't invoke any class or method.
		// To run withod main method we have to user Testng/junit.

		Method2class m2 = new Method2class();
		m2.signIn();
		
		Method.executequery(xpath, 4);

	}
}

//Java variable and Data types
// String int float char boolean false
//private String chrome = "chrome";
//public String mozila = "mozilla";
//protected int myNum = 5;
//static

// (if else),( if ,else if)
// overloading //overriding
//super keyword, this keyword
